import PageShell from "../PageShell";
import ServicesCatalogue from "../ServicesCatalogue";

export default function ServicesPage() {
  return <PageShell eyebrow="Services" title="A service shaped around the business." intro="SHW Digital Services provides practical digital consultancy and delivery. Every contract is holistic and tailored to the client, so the final scope is shaped around the business rather than a fixed package."><ServicesCatalogue /></PageShell>;
}